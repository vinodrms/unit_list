import {AppContext} from '../../../utils/AppContext';
import {AHourlyCronJob} from '../../utils/AHourlyCronJob';

import moment = require('moment')

export class AllotmentArchiverCronJob extends AHourlyCronJob {
	protected runCore(appContext: AppContext) {

	}
}